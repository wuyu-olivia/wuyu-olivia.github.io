x=[[1,0];[0,1];[1,1];[2,1];[6,6]]; %样本集
J = [];
for i = 1: 5 %遍历五种情况
    M(1, :) = sum(x);
    M(1, :) = (M(1, :) - x(i, :)) / 4;
    M(2, :) = x(i, :);
    y = x;
    y(i, :) = [];
    J(i) = sum(sum((y - M(1, :)).^2));%聚类准则函数
end

disp(J);